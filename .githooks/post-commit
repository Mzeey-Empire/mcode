#!/bin/sh
# Repo-local post-commit hook.
#
# The global hook at ~/.git-hooks/post-commit already chains here
# via chain_repo_hook(). Do NOT call back to the global hook from this
# file — that creates infinite recursion (SHLVL spirals to 1000).
#
# Add any repo-specific post-commit logic below this comment.
